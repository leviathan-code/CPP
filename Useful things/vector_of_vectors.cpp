 float x;
      vector<float> cc(stroki);
    for (int i = 0; i < stroki; i++) {
     
        cc.push_back(x);
    
        for (int j = 0; j < stolb; j++) {

            tab.push_back(cc);

        }
    }
